# **Домашнее задание: Развёртывание распределённой системы логирования, хранения и Istio-маршрутизации**

#### Запуск:

```bash
bash ./deploy.sh
```

Скрипт поднимает ресурсы `task1` и затем разворачивает Istio-конфигурацию из `task2`.
